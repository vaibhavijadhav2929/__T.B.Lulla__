import React from 'react';
import SendMessage from './SendMessage';

const Contact = () => {
  return (
    <section id="contact">
      <SendMessage />
    </section>
  );
};

export default Contact;
